# VITE_* variables are public and embedded in the browser bundle. Never put secrets here.
VITE_API_URL=http://localhost:3001
VITE_GOOGLE_CLIENT_ID=

# Future production example; activate only after the API domain exists:
# VITE_API_URL=https://api.yommigo.com